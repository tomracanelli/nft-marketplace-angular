{
  "name": "new-folder",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "front": "npm start --prefix frontend",
    "backend": "npm run dev --prefix backend",
    "start": "concurrently \"npm run front\" \"npm run backend\"",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    
    "concurrent": "^0.3.2",
    "concurrently": "^8.2.2"
  }
}
